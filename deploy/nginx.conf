server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    # Redirect HTTP → HTTPS (uncomment after running certbot)
    # return 301 https://$host$request_uri;

    # --- Static frontend ---
    root /var/www/job-scout-ai/frontend;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # --- API proxy to FastAPI (uvicorn on port 8000) ---
    location /api/ {
        proxy_pass         http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        # Scout runs can take several minutes — don't time out prematurely
        proxy_read_timeout  600s;
        proxy_send_timeout  600s;
    }
}

# --- HTTPS block (uncomment and configure after: sudo certbot --nginx) ---
# server {
#     listen 443 ssl;
#     server_name your-domain.com www.your-domain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     include             /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;
#
#     root /var/www/job-scout-ai/frontend;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api/ {
#         proxy_pass         http://127.0.0.1:8000;
#         proxy_http_version 1.1;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout  600s;
#         proxy_send_timeout  600s;
#     }
# }
